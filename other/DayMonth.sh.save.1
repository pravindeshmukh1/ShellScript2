#!/bin/bash -x

read -p "Enter the Day :-" day
read -p "Enter the Month :-" month

if [$day >= 20 && $month >= 3 && $day <= 31]
then
	echo "true";
elif [$day <= 20 && $month == 6]
then
	echo "true";
elif [$month == 4 || $month == 5 ]
then
	echo "true";
else
	echo "false";
fi
