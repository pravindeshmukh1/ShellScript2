#!/bin/bash -x

read -p "Enter the  year -: " year

if [`expr $year % 4`-eq 0];
then
elif [`expr $$a -eq 0 -a $b -ne 0 -o $c -eq 0year % 100`-eq 0];
then
elif [`expr $year % 400`-eq 0];
then
	echo"yes";
else
	echo"no";
fi
